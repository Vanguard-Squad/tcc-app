<% content_for :title, "Início" %>

<%= render "shared/nav" %>

<main class="page">
  <h1>Olá, <%= current_user.name %>!</h1>
  <p><%= current_company.name %> &middot; <%= role_label(current_user.role) %></p>
</main>
